[package]
name = "mjml_nif"
version = "1.5.0"
authors = ["Paul Götze"]
edition = "2021"

[lib]
name = "mjml_nif"
path = "src/lib.rs"
crate-type = ["cdylib"]

[dependencies]
rustler = "0.30.0"
mrml = { version = "1.2", default-features = false, features = ["parse", "render", "orderedmap"] }
